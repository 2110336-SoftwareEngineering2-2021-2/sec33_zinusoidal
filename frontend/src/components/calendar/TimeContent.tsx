import React from "react";
import styled from "styled-components";
const TimeContent = ({ selectedDay }: any) => {
  return <Layout>TimeContent</Layout>;
};

const Layout = styled.div`
  flex: 1;
  background-color: grey;
`;
export default TimeContent;
